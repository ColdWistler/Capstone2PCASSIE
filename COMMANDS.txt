=== DQN FLIGHT SIM — COMMANDS & ARCHITECTURE ===

--- Run ---
  Normal:    godot --path /home/soggy/Documents/GitHub/Capstone2PCASSIE
  Headless:  godot --headless --path /home/soggy/Documents/GitHub/Capstone2PCASSIE
             (no rendering, 5x time scale, FPS uncapped — much faster training)
             Physics still runs; obstacle detection, raycasts all work.
  Test:      godot --headless --path . -- --test
             (evaluate best model over 10 episodes, greedy actions, no training,
              saves test_report.txt with per-episode stats and summary)

--- Auto-Save ---
  Weights, epsilon, and training stats auto-save every 50 episodes
  to user://dqn_weights.save (Godot's user data folder).

--- Auto-Load ---
  If dqn_weights.save exists at startup, it is loaded automatically
  and training resumes from where it left off.

--- Save Location ---
  Linux:   ~/.local/share/godot/app_userdata/<project>/dqn_weights.save
  Windows: %APPDATA%/Godot/app_userdata/<project>/dqn_weights.save
  macOS:   ~/Library/Application Support/Godot/app_userdata/<project>/dqn_weights.save

--- Training (console output) ---
  Episode N | reward: X.X | steps: M | eps: E.EEE | best: B.B
    N    = episode number
    X.X  = total reward this episode (target: high positive)
    M    = steps in this episode (target: near 2000)
    E.EEE = epsilon (exploration rate, decays from 1.0 to 0.01)
    B.B  = best reward seen so far

--- Episode End Conditions ---
  • Safe landing (parked):     +15 bonus, episode ends successfully
  • Crash:                     0 reward, episode ends
  • Max steps (2000):          episode times out, reward as-is

--- Takeoff (first launch only) ---
  Plane spawns on runway at (0, 2, -250), engine starts, accelerates
  to 30+ m/s, pitches up. DQN takes over above 50 m altitude.
  Subsequent respawns skip takeoff and spawn at altitude.

--- State (12 dimensions) ---
  [forward_speed/100, altitude/500, sin(pitch), cos(pitch),
   sin(roll), cos(roll), vertical_speed/50, is_stalled,
   obstacle_fwd, obstacle_left15, obstacle_right15, fuel_SoC]

--- Actions (7) ---
  0 = nothing
  1 = pitch up
  2 = pitch down
  3 = roll left
  4 = roll right
  5 = throttle up
  6 = throttle down

--- Network (Dueling DQN) ---
  Input(12) -> Hidden(256, ReLU) -> split into:
    - Value stream:   Hidden(256) -> V (scalar)
    - Advantage stream: Hidden(256) -> A (7 values)
  Q(s,a) = V(s) + A(s,a) - mean_a A(s,a)

--- Training Details ---
  • Double DQN:      online network selects best next action,
                     target network evaluates it (reduces overestimation)
  • Soft target:     Polyak averaging (tau=0.005) every step
                     instead of hard copy every N steps
  • Prioritized replay: SumTree with alpha=0.6, beta anneals 0.4->1.0
  • Gradient clip:   all gradients clipped to [-1.0, 1.0]
  • LR decay:        0.001 * 0.9995^step, floor 0.0001
  • Gamma=0.99, Batch=32, Replay=50k, FrameSkip=4, N-step=3,
    TrainInterval=2, ChunkSize=16 (batch split across 2 frames)
  • Optimizer: Adam (beta1=0.9, beta2=0.999, eps=1e-8, bias corrected)
    replaces plain SGD — faster convergence, adaptive per-param LR
  • N-step (3) returns: accumulates rewards over 3 steps before bootstrapping,
    reducing bias and speeding reward propagation
  • Action change penalty: -0.02 when switching actions (reduces jitter)
  • Reward reshaping: altitude uses smooth Gaussian (sigma=80, peak=3.0)
    centered at 200m instead of hard ±50m band; vertical speed dampener
    (-0.05 * |v/s|) near target; hard floor penalty below 50m to prevent
    altitude loss; speed reward capped at 0.3 to reduce competing signal
  • Hidden: 128 -> 256, Batch: 32 -> 64, Replay: 20k -> 50k
  • Landing sequence: auto-pilot triggers at 85% episode (1700 steps).
    Turns toward runway, descends gradually, deploys gear below 80m,
    flares at 15m, touches down. Reward still accumulates during landing.
